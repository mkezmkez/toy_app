<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>User Posts:</strong>
  <% psttt = Micropost.all.find{|p| p.user_id == @user.id}.content %>
  <%= psttt.nil? ? "no posts" : psttt %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
